program MaxValueArray;
var
  numbers,maxValue: array[1..10] of Integer;
  j,i: Integer;
  x: array[6..8] of Boolean;
begin
  write("Enter 10 integer values:");
  for i := 1 to 10 do
  begin
    read(numbers[1]);
  end;
  i :=j;
  j := 1+numbers[1]; 
  maxValue := numbers;
  for i := j+2 to j+5 do
  begin
    if numbers<>maxValue then
    begin
      maxValue := 1;
    end;
  end;
  write("The value is ");  
  write(maxValue);
end.