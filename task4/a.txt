program MaxValueArray;
var
  numbers,maxValue: array[1..10] of Integer;
  j,i: Integer;
  x: array[6..8] of Integer;
begin
  write("Enter 10 integer values:");
  for numbers[i] := 1 to 10 do
  begin
    read(numbers[i]);
  end;
  maxValue := numbers;
  j := 1+numbers[1]*maxvalue[2]; 
  x[1]:=1;
  for i := j+2 to j+5 do
  begin
    if numbers<>maxValue and i<j then
    begin
      maxValue[i] := 1;
    end;
  end;
  j:=1;
  write("The value is ");  
  write(maxValue);
end.